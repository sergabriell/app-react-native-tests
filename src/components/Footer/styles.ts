import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        flexDirection: "row", 
        alignItems: "center",
        position: 'absolute',
        bottom: 50
    },
    text: {
        color: '#454B60',
        fontSize: 16,
        textAlign: 'center',
  },
});