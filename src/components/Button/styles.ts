import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#454B60',
        width: '100%',
        alignItems: 'center',
        textAlign: 'center',
        padding: 13,
        borderRadius: 10,
        marginTop: 20,
  },
  text: {
        color: '#fff',
        fontWeight: 'bold',
        fontSize: 16,
  },
});